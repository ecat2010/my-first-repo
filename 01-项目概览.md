# 第 1 章 项目概览

> 本章导读：本章带你从零认识 Mermaid 这个项目——它是什么、能帮你解决什么麻烦、一共支持画哪些种类的图，以及两个不用安装就能直接上手的好用网站。学完之后，你会明白为什么用「写文本」的方式画图，比用鼠标在软件里拖拽要省心得多，也能对 Mermaid 的全貌有一个清晰印象，为后面的动手实践打好基础。

## 1.1 什么是 Mermaid

你有没有遇到过这种情况：写一份技术文档或项目说明，里面需要画一张流程图，于是你打开一个绘图软件，一格一格地拖框、连线、打字，折腾半天终于画好。结果过了两周，流程改了一小步，你又得重新打开软件、从头再画一遍。更糟的是，别人根本看不出你改了哪里，因为图片就是一张死图。

Mermaid 就是为了解决这类麻烦而生的。

简单说，**Mermaid 是一个用 JavaScript 写成的开源库**。它的特别之处在于：你不需要用鼠标去「画」图，而是用一段「像 Markdown 一样的文本」去「定义」一张图。你把这文本交给 Mermaid，它会自动帮你把文本变成一张可以缩放的矢量图片（SVG）。

为了好理解，我们可以打个比方：

- 你写一段 Mermaid 文本，就像在写一份「食谱」；
- Mermaid 这个库，就像一个「自动炒菜机」；
- 它读完你的食谱，就把菜（图）做出来端到你面前。

你只管把「想要什么菜」用文本写清楚，至于怎么摆盘、线怎么连、框放哪里，机器替你搞定。

这正契合了 Mermaid 官方仓库 README 里反复强调的一个精神：**对抗「文档腐化（doc-rot）」**。所谓文档腐化，指的是文档（尤其是里面的图）随着代码不断变化，却没人同步更新，最后图和实际对不上、越来越过时，变成一堆没人信的「过期废纸」。Mermaid 的思路是：让图和代码用同一种「文本」来写、放在同一个仓库里，跟着代码一起更新、一起进版本控制。这样图就永远不会「偷偷过期」了。

## 1.2 Mermaid 帮你解决了哪些问题

我们再把目光拉回日常，看看 Mermaid 到底解决了哪几个具体痛点。

**第一，非程序员也能轻松画图。** 传统绘图软件（无论是收费的还是免费的）大多依赖鼠标拖拽，要调对齐、调样式、记各种按钮位置，对非技术背景的同学门槛不低。而 Mermaid 只需要你会写「纯文字」——哪怕你完全不懂编程，照着简单语法写几行，就能出图。本质上，它把「画图」变成了「写字」。

**第二，图以图片格式存储时又难改、又难进版本控制。** 很多团队把架构图存成 PNG、JPG 这样的图片文件。一旦要改，得重新打开源文件（如果源文件还在的话）重画一遍；更麻烦的是，图片放进 Git 这类版本工具里时，它是一整块二进制数据，你没法看出「这一版和上一版差了哪根线」，代码评审（code review）也评审不了图片里的内容。

**第三，用文本定义图，就能像代码一样管理。** 因为 Mermaid 的图就是一段文本，它天生可以：

- 直接放进 Git 仓库，和代码待在一起；
- 用 `diff` 工具清楚看到「改了哪一行、哪根连线」；
- 在合并请求（Pull Request / Merge Request）里被同事逐行评审，就像评审代码一样。

一句话总结：Mermaid 把「画图」这件原本靠鼠标、靠图片文件的事，变成了「写文本」这件能被版本控制、能被评审、能跟着代码一起进化的事。

## 1.3 支持的图类型总览

Mermaid 不是只能画一两种图，它目前支持 **30 多种**图类型（这些图的定义代码都整齐地放在仓库的 `packages/mermaid/src/diagrams/` 目录里，每种图一个独立文件夹）。下面我们按用途分几大类，每类各举几个代表，让你有个整体印象。

**一、流程类（描述「步骤和关系」）**

- `flowchart`：流程图，表达步骤之间怎么走、怎么判断分支，最常用。
- `sequence`：时序图，表达多个角色之间随着时间怎么来回发消息。
- `state`：状态图，表达某个东西在不同状态之间怎么切换。

**二、结构类（描述「静态结构」）**

- `class`：类图，软件设计里描述类与类之间的关系。
- `er`：ER 图（实体关系图），描述数据库里表和表的关联。
- `c4`：C4 模型图，从上下文、容器、组件等层次描述系统架构。
- `block`：块图，用方块嵌套表达系统或架构的层次划分。

**三、时间类（描述「时间上的安排」）**

- `gantt`：甘特图，项目管理里排任务、看进度最常用的横道图。
- `timeline`：时间线，按时间顺序罗列关键事件。

**四、数据类（描述「数据分布与流向」）**

- `pie`：饼图，展示各部分占比。
- `xychart`：柱状图 / 折线图，展示数值随横轴变化的趋势。
- `sankey`：桑基图，展示能量、流量或资金从哪来到哪去。

**五、思维类（描述「发散与组织」）**

- `mindmap`：思维导图，把中心主题层层展开成分支。

**六、其他实用图**

- `gitGraph`：Git 分支图，直观展示分支与合并历史。
- `userJourney`：用户旅程图，描述用户使用产品时的步骤与感受。
- `quadrantChart`：象限图，把事物按两个维度放进四个象限。
- `architecture`：架构图，描述系统组件与连接。
- `wardley`：Wardley 地图，用于战略与供应链定位分析。

除了上面这些，仓库里还有 `requirement`、`radar`、`treemap`、`kanban`、`venn`、`swimlanes`、`treeView`、`eventmodeling`、`cynefin`、`ishikawa`、`packet`、`info`、`railroad` 等更多类型。看到这里你大概能感受到：从写代码到做管理、从技术到业务，Mermaid 几乎覆盖了一张开嘴就要「画一张图」的场景。在后面的章节里，我们会先从最常用的流程图、时序图入手，带你真正画出第一张图。

## 1.4 两个重要在线资源

在正式动手之前，先给你介绍两个「不用安装任何东西」就能用的官方资源，它们会贯穿你整个学习过程。

**第一个：官方文档站 `https://mermaid.js.org`**

这是 Mermaid 的「说明书总库」，每一种图的语法、配置项、示例都写得清清楚楚。往后你忘了某个图的写法，第一时间来这里查最权威。

**第二个：在线编辑器 `https://mermaid.live/`**

这是一个官方提供的「在线画图网页」。你打开它，左边写 Mermaid 文本，右边立刻实时显示出图。它最大的好处是：不需要在你电脑上装任何软件，有浏览器、能上网就能用。初学时拿它来试语法、验证想法特别方便。

下面把这个网址交给你身边的智能体，让它帮你在浏览器里打开，你先点进去感受一下界面（这一章先不要求你画，只是认个门）。

> 对智能体说："请帮我在浏览器中打开 Mermaid 在线编辑器，网址是 https://mermaid.live/ 。"

这里顺便说明一个很重要的事：**本教程全程不需要你亲手在终端里敲任何命令**。凡是需要「下载、安装、运行、启动」这类操作，我都会像上面这样，用引号写一句「对智能体说」的话，你只要把引号里的内容原样发给智能体（例如 hermes agent），剩下的交给它去做就行。你只管读、只管理解。

如果一切顺利，浏览器会打开 Mermaid 在线编辑器的页面：左边是文本输入区，右边是图形预览区。哪怕你现在还看不懂里面的文本也没关系，后面我们会一点点拆开讲。

## 本章小结

本章我们认识了 Mermaid 的全貌：

- 它是一个用 JavaScript 写成的开源库，你写「类 Markdown 的文本」定义图，它自动渲染成 SVG 图片，像「写食谱、机器做菜」；
- 它的核心精神是对抗文档腐化，让图和代码用同一种文本语言同步更新、一起进版本控制；
- 它解决了非程序员难画图、图片格式难改难评审三大痛点；
- 它支持 30 多种图，覆盖流程、结构、时间、数据、思维等多个类别；
- 它有两个开箱即用的在线资源：文档站 `https://mermaid.js.org` 和在线编辑器 `https://mermaid.live/`。

读到这里，你可能已经跃跃欲试想自己画一张了。但在动手之前，我们还需要做一些「准备工作」：搞清楚几个关键概念（比如什么是 SVG、为什么用文本），以及了解本教程给你准备的两条上手路线。这正是下一章要讲的内容，请接着看《02-准备工作.md》。
