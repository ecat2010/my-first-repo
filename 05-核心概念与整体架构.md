# 第 5 章 核心概念与整体架构

> 本章导读：前面几章你已经学会安装环境、用 Live Editor 和 Dev Explorer 画出第一张图。本章我们退一步，看看 Mermaid 究竟是怎样把一段文字变成一张漂亮图表的。学完之后，你会理解「解析」与「渲染」的分工、知道 Mermaid 内部用两套解析器、了解决定节点摆位的「布局引擎」、认识主题系统，并拿到一张仓库的「模块地图」。

## 5.1 一张图是怎样「煮」出来的

我们在术语表里把 Mermaid 比作「你写食谱，它帮你煮出菜」。这个类比放到本章正合适。Mermaid 把一张图从文字变成图片，中间大致分三步走：

1. **你写「文本」**：也就是图定义文本（图表源码），例如 `graph LR; A-->B`。这是你喂给 Mermaid 的「原料」。
2. **Mermaid「解析」（parse）**：它先读懂你写的是什么，检查语法对不对，再把文本整理成一份内部的「结构清单」。这一步就像厨师读食谱、确认食材和步骤有没有写错。
3. **Mermaid「渲染」（render）**：它根据那份结构清单，真正把图形画出来，输出成 **SVG（可缩放矢量图）**——一种用代码描述图形、放多大都不糊的图片格式。这一步就像照着食谱下锅，端出成品菜。

所以记住这个顺序：**文本 → 解析 → 渲染 → SVG**。后面所有概念，都是围绕这三者展开的。

## 5.2 两套解析器：Langium 与 Jison

解析这一步最关键。Mermaid 今天支持 30 多种图，但它并不是只用一种技术来做解析。这是一个容易踩坑的地方，请你务必记住：**Mermaid 同时使用了两套解析技术**。

- **Langium**：一种用「文法描述文件」（文件后缀 `.langium`）自动生成解析器的技术。Mermaid 里大约 12 种图用它来实现，这些图的文法写在一个固定位置：`packages/parser/src/language/<图名>/*.langium`。
- **Jison**：一种靠手写语法（写法类似经典的 Bison 工具）来生成解析器的技术。Mermaid 里大约 18 种图用它来实现，文法写在 `packages/mermaid/src/diagrams/<图名>/parser/*.jison`。

为什么有两套？历史原因居多：早期 Mermaid 用 Jison 一种就够；后来社区引入 Langium 这个更现代的方案，新图逐渐迁过去。作为零基础读者，你**不需要**弄懂两套技术谁更先进、怎么写文法。你只要知道一件事：**它们本质都是「把文本语法变成解析器」的工具**，对你来说，无论背后是 Langium 还是 Jison，你写出来的那段图定义文本，最终都会被正确读懂。本章不要求你打开那两个目录，记住「有两套」就达标了。

## 5.3 布局引擎：决定节点摆哪、线怎么绕

解析得到的是「有哪些节点、怎么连」的结构清单，但还没决定「节点画在画布哪个位置、连线怎么绕开障碍」。这个活儿交给**布局引擎**（layout engine）。你可以把它理解成「摆盘师傅」：食材和菜谱都有了，由它决定盘子怎么摆才好看。

Mermaid 主要用到这几个布局引擎：

- **dagre-d3-es**：流程图（flowchart）的默认布局引擎，负责把方框和箭头排得整齐不打架。
- **elk**：另一个强大的布局引擎，放在独立的 `mermaid-layout-elk` 包里。如果你想要更复杂的连线路径，可以在初始化时切换，例如调用 `initialize({ layout: 'elk' })`。
- **tidy-tree**：一种专门把树状结构排得清爽的布局，常用于思维导图等层级图。

布局引擎和解析器是分开的：解析管「是什么」，布局管「放哪里」。这也是为什么同样的图定义，换个布局引擎，出来的样子会不同。

## 5.4 主题系统：图表的整体配色风格

**主题（theme）** 是图表的整体配色风格。Mermaid 内置了好几套：default、base、dark、forest、neutral、neo、redux 等。这些主题的源代码集中放在 `packages/mermaid/src/themes/` 目录里。

主题和前面的解析、布局是「正交」的：解析决定结构，布局决定位置，主题决定颜色。你可以用 `%%{init}%%` 指令在图定义文本里临时覆盖全局配置（例如换主题），也可以在下章讲的 `initialize` 里统一设置。对零基础读者来说，先知道「主题 = 配色风格、有很多套可选」即可。

## 5.5 仓库模块地图

Mermaid 是一个 monorepo（单一大仓库里放多个子包）。如果你以后想翻源码，下面这张「地图」能帮你快速定位（路径都不用手敲，用智能体帮你打开即可）：

- **主库 `packages/mermaid`**：真正发布的库本体（本仓库发布的是 **mermaid 11.x**）。入口文件是 `src/mermaid.ts`，它默认导出一个叫 `mermaid` 的对象——你下一章会频繁调用它的方法。
- **`packages/parser`**：专门放 Langium 那套解析器的地方（见图 5.2）。
- **`packages/mermaid/src/diagrams/`**：每种图一个目录。比如流程图在 `flowchart/`、时序图在 `sequence/`、思维导图在 `mindmap/`。目前这里已经有 30 多种图。
- **渲染相关工具 `rendering-util`**：负责文字测量、布局计算、生成 SVG 等底层活儿，是「渲染」这一步的真正执行者。
- **`themes`**：见 5.4，主题配色文件。
- **`docs`（`packages/mermaid/src/docs/`）**：文档源文件，分 intro / config / syntax / ecosystem 几块。注意：根目录的 `docs/` 是自动生成产物，**不要手改**；要看文档改这里。

> 小提示：在线文档站是 `https://mermaid.js.org`，遇到具体图的语法细节去那里查最权威。

## 5.6 懒加载：为什么首屏只下载用到的图

你可能会担心：30 多种图加起来代码量很大，难道打开一个网页要把全部图都下载下来？不会。Mermaid 通过 `diagram-api/loadDiagram.ts` 实现**懒加载（lazy load）**：当你真正用到某一种图时，它才用动态 `import` 把那一种图的代码拉进来。

这个设计点很关键：它让主库体积保持苗条，用户首屏只加载「实际用到的图」。作为读者你不用关心实现，只要知道 Mermaid 已经替你想好了性能问题——这也是为什么它能在浏览器里轻快运行。

## 本章小结

本章我们厘清了 Mermaid 的「内部厨房」：

- 一张图的生命周期是 **文本 → 解析 → 渲染 → SVG**；
- 解析同时用 **Langium（约 12 种图）** 和 **Jison（约 18 种图）** 两套技术，你只需知道有两套；
- **布局引擎**（dagre-d3-es / elk / tidy-tree）决定节点摆位与连线路径；
- **主题系统** 决定配色，源码在 `themes/`；
- 仓库模块清晰分工，主库入口是 `packages/mermaid/src/mermaid.ts`；
- **懒加载** 让首屏只加载用到的图，体积更小。

到这里，你了解了 Mermaid「背后怎么运作」。但作为使用者，你最常打交道的是它在浏览器里提供的几个接口。下一章《06-浏览器集成与核心 API.md》我们就来讲：怎样在网页里接入 Mermaid，以及 `initialize`、`run`、`render`、`parse` 这几个核心 API 怎么用。
