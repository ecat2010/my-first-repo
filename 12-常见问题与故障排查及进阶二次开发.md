# 第 12 章 常见问题与故障排查及进阶二次开发

> 本章导读：走到这里，你已经掌握了从「画图」到「美化」的全套基本功。但真正动手时，十有八九会撞上几个小坑：图一片空白、报一串看不懂的错、中文变成乱码……本章先把最常见的 6 类问题逐一拆解（现象 → 原因 → 怎么求助/解决），再带你远远看一眼 Mermaid 的「进阶玩法」——外部图插件、本地编辑器、参与开源。学完之后，你不仅能自己排错，还知道往哪里继续深入。

## 12.1 常见问题与排错

下面每个问题都按「现象 → 原因 → 解决话术」来讲。遇到拿不准的，随时把情况原样发给智能体求助。

### 问题 1：图完全不显示

**现象**：页面打开后，该有图的地方要么空白，要么直接把那段图定义文本当普通文字显示出来了。

**原因**，按顺序排查三处：

1. 承载图的元素有没有 `class="mermaid"`？自动渲染只认这个选择器（是 `class="mermaid"`，不是 `<mermaid>` 标签）。如果你写的是 `<pre class="mermaid">图定义</pre>` 或 `<div class="mermaid">图定义</div>`，才算数。
2. 如果你在配置里写了 `startOnLoad: false`，那 Mermaid 不会自动画，必须自己调用 `mermaid.run()` 或 `mermaid.render(...)` 才会出图。
3. Mermaid 脚本到底加载成功没有？CDN 链接写对了吗？请务必用 `mermaid@11` 的链接（版本号写 11.x，不要写成仓库根目录那个 10.2.4）。正确示例：`https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.esm.min.mjs`。

**解决话术**：对智能体说："我的 Mermaid 图完全不显示。我已经确认承载图的元素带了 `class='mermaid'`，配置里 `startOnLoad` 是默认的 true，CDN 用的是 `mermaid@11`。请帮我检查这一段 HTML 哪里还有问题：……（把代码贴上来）。"

### 问题 2：语法报错 / 图是空白

**现象**：控制台报语法错误，或者图渲染出来是空的。

**原因**：图定义文本里有语法写错（比如箭头写错、括号不配对、关键词拼错），Mermaid 读不懂就罢工了。

**解决**：先用 `mermaid.parse(text)` 校验——它只检查语法、不画图，能快速定位错误。把疑似有问题的文本丢给它：

```javascript
mermaid.parse(`graph LR
  A-->B`);
```

如果报错，把报错信息和你的图定义一起贴给智能体：

对智能体说："请帮我校对这段 mermaid 文本的语法错误：\n```\ngraph LR\n  A=>B\n```\n报错信息是：……（贴上报错）。"

### 问题 3：中文 / 特殊字符乱码或字体怪

**现象**：图里的中文变成方块、问号，或者字体和页面其他部分明显不一样。

**原因**有两种：要么是文件没用 UTF-8 编码保存，中文在传输中「水土不服」；要么是图表默认字体没包含中文字形，需要手动指定。

**解决**：先确认你的 HTML / 源文件是以 UTF-8 编码保存的（大多数现代编辑器默认如此，可在编辑器右下角查看）。如果编码没问题但仍不好看，用 `themeVariables` 指定中文字体：

```javascript
mermaid.initialize({
  theme: 'base',
  themeVariables: {
    fontFamily: '微软雅黑'
  }
});
```

对智能体说："请帮我把 Mermaid 图表的默认字体设成‘微软雅黑’，并确保 HTML 文件以 UTF-8 编码保存。"

### 问题 4：节点重叠 / 布局乱

**现象**：流程图里节点挤成一团、连线乱绕、甚至互相压住。

**原因**：要么是图太复杂、节点太多，默认布局引擎算不过来；要么是默认布局引擎（flowchart 用的是 dagre）对这种结构不算友好。

**解决**：flowchart 可以切换到另一种布局引擎 `elk`，往往在复杂大图上更整齐：

```javascript
mermaid.initialize({
  flowchart: { layout: 'elk' }
});
```

如果切了还是很乱，多半是图本身太庞大——试着拆成几张小图，或者用子图（`subgraph`）分组。对智能体说："我的流程图节点太多、布局重叠，请帮我把布局引擎切成 elk，并给个拆分这张大图的建议。"

### 问题 5：点击节点没反应

**现象**：明明给节点加了链接或点击逻辑，点上去却毫无反应。

**原因**：Mermaid 默认 `securityLevel: 'strict'`，出于安全考虑会禁用点击等交互。要交互，必须放开安全级别。

**解决**：

```javascript
mermaid.initialize({
  securityLevel: 'loose'
});
```

设成 `loose` 后，节点上的链接、点击事件才会生效。注意只在你信任图内容来源时这么做（详见《11-配置与主题定制.md》第 11.7 节）。

### 问题 6：想在图里写 HTML 却不显示

**现象**：在节点里写了 `<br>`、`<b>` 这类 HTML 标签，结果要么不显示、要么被原样当成文字。

**原因**：`strict` 模式下 Mermaid 会把图里的 HTML 过滤掉，防止注入风险。

**解决**：要么把 `securityLevel` 改成 `loose`（放开后 HTML 生效），要么用 `themeCSS` 在不写 HTML 的前提下达到类似样式效果。对智能体说："我想在 Mermaid 节点里用 `<br>` 换行并加粗文字，请帮我配置成允许 HTML 的安全级别，并确认这样是否安全。"

## 12.2 进阶二次开发（方向一览）

排错之后，如果你想把 Mermaid「玩得更深」，下面几个方向了解一下即可，不要求现在就上手。

**接入外部图插件**。Mermaid 本体支持 30 多种图，社区还有额外图类型（比如 `zenuml` 这种时序风格图）。这些外部图通过 `mermaid.registerExternalDiagrams(defs, { lazyLoad })` 注册进来，第一个参数是图的定义列表，第二个参数控制是否懒加载。想自己写一个新图类型，可以参考仓库里的 `packages/mermaid-example-diagram` 这个示例包，它是官方给的「最小可运行模板」。

**本地实时编辑器**。不想装任何东西，直接打开官方在线编辑器 `https://mermaid.live/`，左边写文本、右边实时出图，最适合练手。如果你把 Mermaid 仓库（D 盘的 mermaid-develop 文件夹）下载到了本地并装好了依赖，也可以让智能体帮你启动开发服务器，然后在浏览器打开 `http://localhost:9000/dev/` 的 Dev Explorer——那是仓库自带的本地调试界面，能实时编辑 `.mmd` 文本并看图，还能翻看 `demos/` 里每种图的演示页。

> 对智能体说："请在 mermaid-develop 仓库里启动开发服务器，然后帮我在浏览器打开 `http://localhost:9000/dev/` 的 Dev Explorer 界面。"

**参与开源贡献**。这个仓库用 `pnpm` 管理依赖、用 `changesets` 这套工具来管理版本号——你改完代码，它会帮你自动生成版本变更记录。改完之后向官方提一个 PR（Pull Request）就行。本教程不教具体的 git 操作，你只需把改动告诉智能体，让它协助你整理并提交即可。

**查资料去处**。遇到文档里没讲的细节，认准这几个官方入口：在线文档站 `https://mermaid.js.org`、GitHub 仓库（搜 mermaid-js/mermaid）、以及官方 Discord 社区。文档站的源文件就在仓库的 `packages/mermaid/src/docs/` 下，分 intro / config / syntax / ecosystem 几块，想深挖配置默认值可以顺藤摸瓜。

## 本章小结与全书结语

这一章我们做了两件事：一是把新手最常踩的 6 个坑（不显示、报错、乱码、布局乱、点击无反应、HTML 不生效）逐一拆开讲清，并给了你「直接发给智能体」的求助话术；二是远远指了三条进阶路——外部图插件、本地 Dev Explorer、开源贡献。

至此，这套《Mermaid 零基础中文教程》就全部结束了。我们一起走过了这样一段路：

1. **认识 Mermaid**：它是一个用 JavaScript 写、把「类 Markdown 文本」自动画成 SVG 图片的开源库——你写食谱，它帮你煮出菜。
2. **快速开始**：通过 CDN 引入，无需安装就能在网页里画图；也了解了把仓库下载到本地的方式。
3. **核心 API**：`mermaid.initialize` 设配置、`mermaid.run` 自动渲染、`mermaid.render` 手动出图、`mermaid.parse` 校验语法。
4. **各种图语法**：流程图、时序图、类图、状态图、甘特图、饼图、思维导图……30 多种图随你挑。
5. **配置与美化**：用 `theme`、`look`、`themeVariables`、`themeCSS` 和 `%%{init}%%` 把图调成你想要的模样。
6. **排错与进阶**：遇到问题知道往哪查，想深入也知道往哪走。

Mermaid 的魅力在于：你越写越会发现，很多本来要打开画图软件、拖半天的图，现在敲几行文字就出来了。最省事的练习场，就是官方在线编辑器 `https://mermaid.live/`——打开它，把教程里任何一段例子贴进去，改几个字看看图怎么变，你就算真正「上手」了。

祝你画图愉快，也欢迎你把自己的图分享给更多人。
