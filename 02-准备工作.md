# 第 2 章 准备工作

> 本章导读：在亲手画出第一张图之前，我们先把「地基」打牢。本章会先扫清几个必须懂的基础概念（什么是用文本定义图、为什么文本比图片好、SVG 是什么），再带你认识 Mermaid 的两种运行方式，最后给你一份贯穿全书的「关键词对照表」。学完之后，你会清楚自己有哪两条上手路线可选，也能在后面章节遇到陌生词时，回来这里快速查意思。

## 2.1 前置概念扫盲

在往下走之前，有三个概念你必须先弄明白，它们会反复出现在整本教程里。

**概念一：什么是「用文本定义图」**

前面说过，Mermaid 不让你用鼠标拖框连线，而是让你写一段「类 Markdown 的文本」来描述一张图。我们把这段用来定义图的文字，叫做**图定义文本**（也可以叫「图表源码」）。它就好比是「喂给 Mermaid 的原料」。

举个例子，下面这段就是一段图定义文本（注意：这只是文本，不是图片）：

```text
graph LR
    A[开始] --> B[做事]
    B --> C[结束]
```

你一看到 `A[开始] --> B[做事]` 就能读懂意思：有一个「开始」节点，连到「做事」节点。Mermaid 读进这段文本，就能画出一张从左到右的流程图。记住：**你写的是文字，出来的是图。**

**概念二：为什么要用文本，而不是图片**

这正是第 1 章提过的核心优势，这里再明确归纳三点：

- **版本可控**：文本能直接放进 Git，和代码待在同一仓库，跟着一起提交、一起历史回溯；
- **容易修改**：要改图，只要改那几行文字，不用重新打开绘图软件重画；
- **可 code review**：因为图是文本，同事在合并请求里可以逐行看「哪根线改了、哪个框名改了」，就像评审代码一样清清楚楚。

图片（PNG/JPG）做不到这些——它是死的一整块，看不出改动，也很难评审。

**概念三：SVG 是什么，为什么重要**

Mermaid 画出来的图，格式是 **SVG（可缩放矢量图）**。这是一种用代码来描述图形的图片格式，和我们平时拍的照片（位图）不同：位图放大到一定程度会糊成一堆马赛克，而 SVG 是用「点、线、形状的坐标」来记录的，所以**你把它放多大都不会模糊**。

这对图表特别友好：无论你在手机小屏上看，还是投到会议室大屏上，线条和文字都始终清晰。知道这一点，你就明白为什么 Mermaid 选择输出 SVG 而不是普通图片了。

## 2.2 运行环境的两条路

要使用 Mermaid，其实有两条完全不同的路线。我们先看清区别，后面你才知道自己该走哪条。

**路线一：在浏览器里直接用（靠 CDN，零安装）**

CDN 是「内容分发网络」的简称，你可以把它想象成一个网上的「公共厨房」：Mermaid 已经做好、摆在那儿了，你不用在自己电脑上安装，只要在网页里引用一个网上链接，就能直接用。

这种方式的好处是：你电脑上什么都不用装，打开浏览器、写段文本就能出图。对只想「画张图看看」的同学来说，这是最轻松的路。我们会在《04-快速上手.md》里带你走这条路。

**路线二：在本地开发、改 Mermaid 源码（需要装运行环境）**

如果你不只是想用，还想把整个 Mermaid 仓库下载到本地、运行它的演示页、甚至改它的源代码，那就需要两套本地「发动机和食材」：

- **Node.js**：一个让 JavaScript 脱离浏览器也能运行的环境，相当于 Mermaid 这台机器的「发动机」。Mermaid 库本身和开发环境都离不开它。本仓库要求使用 **Node 24.x**。
- **pnpm**：一个管理 JavaScript 项目「食材清单（依赖）」的工具，比老牌的 npm 更先进、更省空间。你可以把它理解成「依赖管家」——项目运行前需要先备齐的那些现成代码包（也就是**依赖**），由它来帮你下载和管理。本仓库使用 **pnpm 10.x**。

一句话区分：只想画图 → 走路线一，零安装；想玩源码、跑仓库 → 走路线二，需要先在本机备好 Node.js 和 pnpm。

这里再提醒一次：**本教程所有的「下载、安装、运行」动作，都不用你亲手敲命令**。你只需要把下面这样的「对智能体说」原样发给智能体即可。比如想先看一眼官方文档，可以这样：

> 对智能体说："请帮我在浏览器中打开 Mermaid 官方文档站，网址是 https://mermaid.js.org 。"

如果一切顺利，浏览器会打开文档站首页，左侧是各种图的分类菜单，右侧是详细语法说明。这一章你先不用细读，认个地方就好。

## 2.3 本教程的两条主线

把上面两条路线对应到本书结构，你学习时有两条清晰的主线：

**主线 A：用 CDN 零安装，直接画图**

这条线完全不需要在本机装任何东西。你只要打开一个能引用 CDN 的网页（或用前面认识的在线编辑器），写段文本就能出图。它最适合「我就想快点画张图」的需求，对应的实战内容在《04-快速上手.md》。

**主线 B：把仓库下载到本地，跑预览、改源码**

这条线适合想深入 Mermaid、看它自带演示、甚至参与开发的同学。走这条线，你需要先按《03-获取与安装.md》把 Node.js、pnpm 装好，再把仓库拉到本地、安装依赖、启动本地预览服务（比如仓库自带的演示页和 Dev Explorer 调试界面）。这条路稍重一些，但能让你看到 Mermaid 全貌、动手改东西。

你可以根据自己目标任选一条开始，两条线在「怎么写图定义文本」这件事上是相通的——文本语法学会了，换哪儿都能用。本书会先带你把环境准备好（第 3 章），再用最轻松的方式画出第一张图（第 4 章）。

## 2.4 关键词对照表

为了让你在后面章节遇到陌生词时不慌，这里把全书高频词一次性对照清楚（定义均与官方术语表保持一致）：

- **Mermaid**：一个用 JavaScript 写成的开源库，你写「类 Markdown 的文本」定义图，它自动帮你渲染成 SVG 图片。类比：你写食谱，它帮你煮出菜。
- **图定义文本 / 图表源码**：用 Mermaid 语法写的那段文字（例如 `graph LR; A-->B`），是「喂给 Mermaid 的原料」。
- **渲染（render）**：Mermaid 把文本变成可见图片（SVG）的过程。
- **解析（parse）**：Mermaid 先读懂你写的文本、检查语法对不对、整理成内部结构的过程，是渲染的前一步。
- **SVG（可缩放矢量图）**：用代码描述图形的图片格式，放多大都不模糊，Mermaid 输出的就是 SVG。
- **Node.js**：让 JavaScript 不用浏览器也能运行的环境，像 JS 的「发动机」。Mermaid 库和开发环境都需要它。
- **pnpm**：管理 JavaScript 项目「食材清单（依赖）」的工具，比 npm 更先进。本仓库用 pnpm 10.x。
- **依赖（dependency）**：项目运行需要的其他现成代码包，像做菜前要先备齐的食材。
- **CDN（内容分发网络）**：网上的「公共厨房」，你不自己装 Mermaid，直接引用网上链接就能用。
- **ESM / IIFE**：JavaScript 的两种打包格式。简单记一句话——**浏览器 standalone 版**就是 `mermaid.min.js`（IIFE 全局构建，用 `<script>` 标签引入、挂到全局变量 `mermaid`）；**ESM CDN** 用的是 `mermaid.esm.min.mjs`（用 `import` 方式引入，适合现代打包器）。本教程用到 CDN 时，标准写法是 `https://cdn.jsdelivr.net/npm/mermaid@11/dist/mermaid.esm.min.mjs`。

把这张表收藏好，后面哪章卡在名词上，回来扫一眼就通了。

## 本章小结

本章我们做好了动手前的全部准备：

- 扫清三个基础概念：图定义文本（喂给 Mermaid 的原料）、为何用文本（版本可控、易改、可 code review）、SVG（放大不模糊的矢量图）；
- 看清运行环境的两条路：浏览器靠 CDN 零安装直接用，本地开发则需要 Node.js（发动机，本仓库用 24.x）加 pnpm（依赖管家，本仓库用 10.x）；
- 明确本教程两条主线：A 用 CDN 零安装直接画图（见《04-快速上手.md》），B 把仓库下到本地跑预览/改源码（见《03-获取与安装.md》）；
- 附上一份高频关键词对照表，涵盖 Mermaid、图定义文本、渲染、解析、SVG、Node.js、pnpm、依赖、CDN、ESM/IIFE 等。

铺垫到此结束。下一章我们就正式进入「安装与本地运行」：如果你只想走主线 A 零安装画图，可以略读；如果你想把仓库跑起来玩源码，请跟着《03-获取与安装.md》一步步来。
