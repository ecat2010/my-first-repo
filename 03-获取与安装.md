# 第 3 章 获取与安装

> 本章导读：在正式画图之前，我们需要先把 Mermaid 的源代码仓库拿到本地，并准备好它运行所需的"食材"。本章带你完成三件事：把仓库下载到本地、安装项目依赖、按需构建项目并认识构建产物。学完之后，你将在 `D 盘的 mermaid-develop 文件夹` 里拥有一份完整的 Mermaid 源码，并理解那些以 `mermaid` 开头的文件各自是做什么用的。

在开始之前，先说明一件重要的事：**本教程不需要你亲手去敲任何终端命令**。你只要把下面带引号的话原样发给智能体（例如 hermes agent），由它替你完成下载、安装、构建等操作即可。你只需要坐在电脑前，看着事情发生，并理解每一步在做什么。

---

## 3.1 下载仓库

Mermaid 是一个开源项目，它的全部源代码都托管在 GitHub 上。所谓"仓库"，可以理解为一整个装满代码、文档、示例的文件夹。我们要做的第一件事，就是把这个文件夹完整搬到你电脑的本地硬盘上。

如果你之前还没有下载过，请对你的智能体说：

> 对智能体说："请帮我把 GitHub 上的 mermaid 仓库下载到本地 D 盘的 mermaid-develop 文件夹。"

智能体会自动从 GitHub 把仓库拉取（即下载）到你指定的位置。等它完成后，你的电脑上就会多出 `D 盘的 mermaid-develop 文件夹`，里面是一整套 Mermaid 的源码。

**如果你已经拥有本地文件夹（也就是 D 盘的 mermaid-develop 文件夹）**（比如之前已经下载过，或是别人拷给你的），那么这一步可以直接跳过，无需重复下载——仓库已经在手，我们直接进行下一步即可。

> 小提示：Mermaid 的版本号，教程统一写为 **mermaid 11.x**。你会看到仓库根目录里某个文件写着一个 `10.2.4` 的数字，那是整个大仓库的"元版本号"，并不代表真正发布的库版本；真正发布的库是 11.x，所以请始终以 11.x 为准。

---

## 3.2 安装依赖

拿到仓库只是第一步。一个现代 JavaScript 项目，往往不是孤零零一份代码，而是"站在巨人的肩膀上"——它要调用很多别人已经写好的现成代码包，这些代码包就叫做**依赖（dependency）**。

打个比方：依赖就像做菜前要先备齐的食材。Mermaid 这道"大菜"需要几十种"食材"（比如负责画图的图形库、负责排版的布局引擎等），一样都不能少。如果我们自己一个一个去找、去装，既容易出错又极其繁琐。这时候就需要一个"采购管家"来帮我们一次性买齐，它的名字叫 **pnpm**。

**pnpm** 是一个专门管理 JavaScript 项目"食材清单"的工具。它会读取仓库里一份叫 `pnpm-lock.yaml` 的清单文件，按清单把项目所需的全部依赖一键装好。本仓库使用的 pnpm 版本是 **pnpm 10.x**，它运行的环境 **Node.js**（一个让 JavaScript 脱离浏览器也能运行的"发动机"）要求是 **Node 24.x**——这些都是仓库自己定好的，智能体会自动适配，你不必操心。

请对你的智能体说：

> 对智能体说："请帮我在 D 盘的 mermaid-develop 文件夹里，用 pnpm 安装这个项目所需的全部依赖。"

智能体会进入该文件夹，读取 `pnpm-lock.yaml`，然后把所有依赖下载并摆放到位。等它提示完成，你就拥有了一个"食材齐全、随时可下锅"的项目。

如果一切顺利，你会看到智能体反馈依赖安装完成的提示信息，且文件夹里多出一个 `node_modules` 目录，那就是依赖们住的地方。

---

## 3.3 构建项目（可选）

所谓**构建**，通俗地说，就是把程序员写的、分散在几十个文件里的原始源代码，"编译打包"成一份浏览器能直接读懂、能直接用的成品文件。就像把散装零件组装成一台可以出厂的机器。

这里有一个好消息：**构建并不是必须的**。如果你只是想预览仓库里自带的示例图（`demos/` 里的演示页），往往不需要先构建，直接启动开发服务器就能看（详见《04-快速上手.md》的 4.2 节）。构建主要用在你想要得到一份可以在浏览器里独立引用的成品文件时。

如果你确实需要构建一份成品，请对你的智能体说：

> 对智能体说："请帮我构建 mermaid 项目，生成可在浏览器使用的文件。"

智能体会执行构建流程，把分散的源码加工、压缩、打包。等它完成后，仓库里会多出一个 `dist` 文件夹（dist 是 distribution 的缩写，意为"分发物"），里面就是构建产物——也就是下面 3.4 节要认识的那些 `mermaid` 开头的文件。

> 注意：根目录下的 `docs/` 文件夹是文档的自动生成产物，**请不要手动修改它**。如果想调整文档或默认值，应当去改动源文件，而不是手改生成出来的 `docs/`。

---

## 3.4 认识构建产物

构建完成后，`dist` 文件夹里会出现好几个名字相似的 `.js` / `.mjs` 文件。初见它们很容易迷糊，但这里我们把它们逐一讲清。先记住两个关键概念：

- **ESM**（ES Modules）：一种现代的 JavaScript 打包格式，适合被打包器通过 `import` 方式引用，常用于 CDN 或现代前端工程。
- **IIFE**（立即执行函数表达式）：一种"老派但好用"的格式，可以直接用 `<script>` 标签引入，并自动挂到一个全局变量 `mermaid` 上，浏览器里拿来即用。

下面逐个认识（文件路径都在 `dist` 文件夹内）：

- **`mermaid.esm.min.mjs`**：ESM 格式的压缩版。文件名里的 `min` 表示"压缩过、体积小"，最适合通过 CDN 在网页里引用。我们在第 4 章的"CDN 零安装"方式里用的就是它。
- **`mermaid.esm.mjs`**：ESM 格式、但把依赖也一并打包进去了的版本。体积比上面的 `min` 版大，适合需要"自带依赖、单文件搞定"的 ESM 场景。
- **`mermaid.core.mjs`**：ESM 格式、但把外部依赖"留白"的版本。它假设你会在外面用打包器把依赖补上，专门给打包器做"二次打包"用，不是给普通网页直接用的。
- **`mermaid.min.js`**：**IIFE 全局构建**，也就是教程里常说的"浏览器 standalone 版"（独立可用版）。引入后会在全局挂上 `mermaid` 这个变量，浏览器里无需任何打包工具即可使用。
- **`mermaid.js`**：对应 `mermaid.min.js` 的未压缩版，方便调试时阅读源码，体积较大，日常使用一般选压缩版。
- **`mermaid.tiny.min.js`**：精简版（tiny = 小巧），只打包了最常用的图类型，体积更小，适合只想画几种基础图的轻量场景。

这里要特别强调一个**避坑要点**：网上有些旧资料会提到 `mermaid.standalone.js` 这个名字，但**在 Mermaid 11.x 里并不存在这个文件**。所谓"standalone 浏览器独立版"指的就是上面说的 `mermaid.min.js`（IIFE 全局构建），请不要去找一个并不存在的 `mermaid.standalone.js`。

---

## 本章小结

本章我们完成了使用 Mermaid 源码前的三项准备：

1. **下载仓库**：把 GitHub 上的 mermaid 仓库搬到了 `D 盘的 mermaid-develop 文件夹`（若已有则跳过）。
2. **安装依赖**：借助 pnpm 10.x（运行于 Node 24.x 环境）按 `pnpm-lock.yaml` 清单一键装齐所有"食材"。
3. **构建与识货**：构建可选，产物都在 `dist` 文件夹；重点分清了 ESM 系（`mermaid.esm.min.mjs` 等，供 CDN/打包器）与 IIFE 系（`mermaid.min.js`，浏览器 standalone 版），并记住没有 `mermaid.standalone.js` 这个文件。

至此，你的电脑里已经有了一份完整、可用的 Mermaid 源码。下一步，我们就来看看怎么真正动手画第一张图——不需要复杂配置，两种方式任选其一，最快几分钟就能看到成果。请继续阅读《04-快速上手.md》。
